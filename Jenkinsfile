pipeline {
    agent any

    stages {
        stage('Clone stage') {
            steps {
                git branch: 'main', url: 'https://github.com/Dechateau2035/datacamp_docker_angular.git'
            }
        }
    }
}
